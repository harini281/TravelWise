# ASP.NET Core API Environment Variables
DATABASE_URL=Host=localhost;Port=5432;Database=travelwise;Username=postgres;Password=postgres
JWT_SECRET=travelwise_super_secret_jwt_key_for_university_evaluation_2026!
AISERVICE_URL=http://localhost:8000
ASPNETCORE_ENVIRONMENT=Development
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# Backend process environment only. Never use VITE_ or commit your actual key.
# Free key: https://myprojects.geoapify.com/ (no payment details required).
# Export locally or set in Render Environment; ASP.NET does not auto-load this example file.
GEOAPIFY_API_KEY=
# Discovery only: dates/guests do not check availability. No paid inventory integration.
# Guard: 4 outbound requests/sec, 2800 conservative credits/UTC day per process.
# Counters reset on restart; use one backend instance and monitor the free account quota.
